# Lab 1 — Data Pipeline with Enhancements

A hands-on data pipeline built on the WikiText-2 dataset using HuggingFace `datasets`, `transformers`, and PyTorch. The lab covers loading, tokenizing, chunking, and batching text data for causal language model training — extended with seven enhancements that add analysis, visualization, and performance benchmarking.

---

## Quick Start

```bash
# 1. Clone the repo
git clone https://github.com/your-username/your-repo-name.git
cd your-repo-name

# 2. Install dependencies
pip install transformers datasets torch matplotlib

# 3. Open the notebook
jupyter notebook Data_lab_1.ipynb
# or open in Google Colab (recommended — no local setup needed)
```

> **Recommended environment:** Google Colab (free GPU, all HuggingFace libraries pre-installed).  
> All cells run top-to-bottom with no extra configuration.

---

## Dataset

| Property | Value |
|---|---|
| Dataset | WikiText-2 (raw) |
| Source | `wikitext-2-raw-v1` via HuggingFace Hub |
| Train split | 36,718 lines |
| Task | Causal language model pre-training |

---

## Notebook Structure

### Original Pipeline (Cells 1–3, 7, 11, 17, 21, 25)

The core assignment — a complete data pipeline from raw text to a PyTorch `DataLoader` ready for GPT-2 training.

| Step | What it does |
|---|---|
| 1 | Load WikiText-2 training split from HuggingFace |
| 2 | Initialize GPT-2 tokenizer; set pad token to EOS |
| 3 | Tokenize full dataset using batched `.map()` |
| 4 | Chunk all tokens into fixed 128-token sequences (18,667 total) |
| 5 | Build PyTorch `DataLoader` with custom `collate_fn`; labels = input_ids |
| 6 | Sanity check — confirm batch shape is `[8, 128]` |

---

### Enhancements

#### Enhancement 1 — Dataset Statistics Before Tokenization *(Cell 5)*
Computes word count per line across all non-empty examples and plots a log-scale histogram. Reveals that most WikiText-2 lines are very short (section headers, blank lines) with a long tail of full paragraphs.

#### Enhancement 2 — Multi-Tokenizer Comparison *(Cell 9)*
Loads GPT-2, BERT, and DistilBERT tokenizers and compares how many tokens each produces for three test sentences — including a deliberately long word. Printed as a table showing vocab size alongside token counts.

```
Model                Vocab Size   Tokens (s1)    Tokens (s2)    Tokens (s3)
---------------------------------------------------------------------------
GPT-2                50257        12             10             12
BERT                 30522        12             10             14
DistilBERT           30522        12             10             14
```

#### Enhancement 3 — Vocabulary Frequency Analysis *(Cell 13)*
Counts token ID frequency across the entire tokenized dataset. Reports total tokens, unique tokens used vs. full vocab size, and plots the top 20 most frequent tokens as a bar chart.

#### Enhancement 4 — Block Size Comparison *(Cell 15)*
Runs the chunking step at three block sizes (128, 256, 512) and compares the resulting sequence counts. Larger blocks = fewer but richer sequences.

| Block size | Sequences produced |
|---|---|
| 128 | 18,667 |
| 256 | 9,333 |
| 512 | 4,666 |

#### Enhancement 5 — Attention Mask Visualization *(Cell 19)*
Renders the first 16 sequences' attention masks as a heatmap (blue = real token, white = padding). Since WikiText-2 is chunked to fixed length, padding fraction is very low — this is confirmed quantitatively.

#### Enhancement 6 — Multi-Worker DataLoader Benchmark *(Cell 21)*
Times 50 batches at `num_workers=0` vs `num_workers=2` and reports throughput in batches/sec. The primary `train_loader` uses `num_workers=2` for the rest of the notebook.

#### Enhancement 7 — Streaming Mode *(Cell 23)*
Implements a custom `StreamingTokenizedDataset` (PyTorch `IterableDataset`) that loads WikiText-2 in streaming mode, tokenizes on-the-fly, and yields fixed-length chunks — no full download required. Demonstrates how to swap batch loading for memory-efficient streaming.

---

### Wrap-Up Visualizations (Cells 27–29)

#### Cell 27 — Tokenizer Comparison Bar Chart
Grouped bar chart showing token counts per sentence across GPT-2, BERT, and DistilBERT. Makes the tokenizer comparison from Enhancement 2 visual.

#### Cell 28–29 — Pipeline Summary Dashboard
A 2×2 figure summarizing the entire lab at a glance. See below.

---

## Pipeline Summary Dashboard

> Generated by Cell 29 at the end of the notebook.  
> To reproduce: run all cells top-to-bottom, then add the following save line before `plt.show()` in Cell 29:
>
> ```python
> plt.savefig("pipeline_summary.png", dpi=150, bbox_inches="tight")
> ```
>
> Then commit the image alongside the notebook:
> ```bash
> git add pipeline_summary.png
> git commit -m "Add pipeline summary dashboard"
> git push
> ```


| Panel | What it shows |
|---|---|
| Top-left | Raw counts at each pipeline stage: total lines → non-empty lines → LM sequences → unique tokens |
| Top-right | How sequence count changes across block sizes 128 / 256 / 512 |
| Bottom-left | DataLoader throughput: `num_workers=0` vs `num_workers=2` in batches/sec |
| Bottom-right | Vocab size per tokenizer (bars) with WikiText-2 coverage % overlaid as a line |

---

## File Structure

```
.
├── Data_lab_1.ipynb       # Full notebook with all enhancements
└── README.md              # This file
```

---

## Dependencies

```
torch
transformers
datasets
matplotlib
```

Install all at once:
```bash
pip install transformers datasets torch matplotlib
```

---

## Key Takeaways

- **Chunking strategy matters** — block size directly controls the quantity vs. context-richness tradeoff in training sequences.
- **Tokenizers differ** — GPT-2 uses BPE with a 50K vocab; BERT/DistilBERT use WordPiece with a 30K vocab. Token counts per sentence are similar but diverge on rare/long words.
- **Streaming is viable** — for large datasets, streaming avoids upfront memory cost entirely at the expense of random shuffling.
- **Multi-worker loading helps** — even `num_workers=2` gives a measurable throughput improvement over single-threaded loading.
